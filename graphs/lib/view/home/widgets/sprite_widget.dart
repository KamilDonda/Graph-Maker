import 'package:flutter/material.dart';

class SpriteWidget extends StatelessWidget {
  const SpriteWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox();
  }
}
